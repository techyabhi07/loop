/*write a program to find the factorial value of a number*/

#include<stdio.h>

int main(){
    int num,i,fact;
    printf("enter the number;");
    scanf("%d",&num);
    fact==i= 1;
    while (i<=num)
    {
        fact=fact*i;
        i+=
    }
    
return 0;
}
